# تعديل عرض رقم الوحدة في الإيصال

الهدف: إزالة الإطار (التظليل) من رقم الوحدة في الإيصال التلقائي عند تسجيل دفعة جديدة.

## الخطوات

1. في `src/pages/Payments.tsx`:
   - حذف CSS class `.unit-pill` من `<style>` (سطر ~244)
   - تعديل HTML من `<span class="unit-pill">#${...}</span>` إلى `#${...}` (سطر ~260)

## النتيجة
رقم الوحدة يظهر كنص عادي **#5** بدون خلفية ملونة أو إطار.

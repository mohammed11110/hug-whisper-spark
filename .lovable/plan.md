## الخطة: إخفاء قيمة الإيجار للوحدات الشاغرة

### التعديل المطلوب
في `src/pages/BuildingDetail.tsx`، تعديل سطر عرض معلومات الوحدة ليتجاهل قيمة الإيجار ونوع الدورة إذا كانت حالة الوحدة `vacant` (شاغرة).

### الملف المعدل
- `src/pages/BuildingDetail.tsx` — تعديل سطر واحد ضمن `visible.map()`.

### التوقع بعد التنفيذ
الوحدة الشاغرة تظهر فقط بنوعها (مثلاً "شقة") بدون عرض السعر (مثلاً "500/شهري").
## تحسين العربية في خانات (المتأخرون / الدافعون / الدفعات الجزئية)

الصياغة الحالية ركيكة بعض الشيء وغير متّسقة. سأحدّث المفردات لتكون أكثر فصاحة ومناسبة لهجة عقارية محترفة.

### التغييرات في `src/lib/i18n2.tsx`

| المفتاح | الحالي | الجديد |
|---|---|---|
| `paid_tenants` | الدافعون | المسدِّدون |
| `late_tenants` | المتأخرون | المتأخرون عن السداد |
| `partial_payment` | دفع جزئي | سداد جزئي |

### التغييرات في `src/pages/MonthlyCollection.tsx`

- **بطاقة العداد** (السطر 196، 201): إبقاء `t2("paid_tenants")` و `t2("late_tenants")` لتعكس النصوص الجديدة تلقائياً، مع تغيير التسمية المختصرة في العداد إلى:
  - "مسدِّدون" بدل "الدافعون" (نسخة مختصرة للعداد فقط)
  - "متأخرون" بدل "المتأخرون" (نسخة مختصرة للعداد)
- **عناوين الأقسام** (207، 232): استبدال الإيموجي بصياغة أنظف:
  - `⚠️ المتأخرون (3)` → `المتأخرون عن السداد · 3`
  - `✅ الدافعون (5)` → `المسدِّدون · 5`
  - `📋 الدفعات (10)` → `الدفعات المُستلمة · 10`
- **تسمية المتبقي** (السطر 216): "متبقي إيجار هذا الشهر" → "المتبقي لهذا الشهر"
- **سطر السداد الجزئي** (السطر 217): `${t2("partial_payment")}: ${format(...)}` → `سُدِّد جزئياً: ${format(...)}`
- **زر التحصيل السريع** (السطر 222): إبقاء `quick_collect` كما هو ("تحصيل سريع")

### الملفات المعدّلة

- `src/lib/i18n2.tsx`
- `src/pages/MonthlyCollection.tsx`

لن تتغيّر النصوص الإنجليزية ولا أي منطق أو استعلامات قاعدة بيانات.
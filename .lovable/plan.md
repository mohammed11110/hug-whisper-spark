## الهدف
تغيير ترتيب قائمة المدفوعات إلى **من الأجدّ إلى الأقدم** (حسب تاريخ الدفع) بدلاً من الترتيب الحالي حسب رقم الإيصال.

## التغيير

### `src/pages/Payments.tsx`
- استبدال استدعاء `compareReceiptDesc` داخل `filtered` بفرز حسب `payment_date` تنازلياً، ثم `id` كمعيار ثانوي لاستقرار الترتيب عند تساوي التاريخ.
- حذف الدالتين `extractReceiptNum` و`compareReceiptDesc` لأنهما لم تعودا مستخدَمتَين.

## ملاحظات
- استعلام `load()` يبقى مرتّباً بـ `payment_date desc` (لا تغيير).
- لا تغيير على قاعدة البيانات.
- ينطبق على كل المباني/المستأجرين.

## ملفات ستُعدَّل
- `src/pages/Payments.tsx`
